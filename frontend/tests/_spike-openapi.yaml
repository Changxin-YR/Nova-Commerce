openapi: 3.0.0
info: { title: spike, version: "1.0.0" }
paths: {}
components:
  schemas:
    Envelope:
      type: object
      properties:
        code: { type: integer }
        message: { type: string }
        data: { }
        trace_id: { type: string }

