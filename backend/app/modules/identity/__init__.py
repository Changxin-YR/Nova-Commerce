"""Identity context: accounts, auth sessions, RBAC and data scope.

Spec sections 21-24 and 104. Also the owner of the DataScope primitive that
INV-010 (agent permission <= user permission) and INV-012 (MCP scope cannot
bypass RBAC/DataScope) both depend on.
"""
